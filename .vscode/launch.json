{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}",
      "args": [
        // "-config", "config.yaml",
        "-mode", "evaluate",
        // "-repo", "myapp"
      ]
    }
  ]
}
